//arrays

const arr = [1, 2, 3];

arr.length

console.log(arr[0]);

	arr[4] = 2;

	arr.push(1) --> // add item to the end of the array
	arr.pop() --> // remove something from end of the array
	arr.unshift() --> // puts it in the beginning
	arr.shift()  --> // removes from the beginning of array
	arr.reverse()  --> // flips the array around
+=
a = a +  1
-=
a = a - 1
/=
a = a / 1
*=
a = a * 1
%=

//you can put an array into an arry.  you can incept objects within objects

// %=

//{} object

const turtle = {
	name : 'marvin',
	age: 102,
	hungry: true
};
//always try to use strings all the time when clasifying
turtle.name = 'name' //changes the value
turtle['hungry'] = false; //also can change the value
  
const bird = {};

bird.name = 'dee';

const daniel= {};
daniel.age = 25
daniel.hobby = "golf"
daniel.state = "tired"
daniel.location = "chicago"
daniel.shirtcolor = "grey"

daniel.lower()

const family = {
    father = "steve"
    mother = "chung"
	sister = "ester"
	cousin = "john"
	gf = "hyojin"};
















